<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- title %></title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <header>
        <h1>AlkHost -dev</h1>
        <p>Entorno de desarrollo web local -Servidor web- de aplicaciones estaticas</p>
    </header>

    <main>
        <div class="main-page">
            <h1>Bienvenido a AlkHost</h1>
            <section class="projects-section">
                <h2>Tus Proyectos</h2>
                <div class="projects-grid">
                    <% projects.forEach(function(projectName) { %>
                        <div class="project-card">
                            <h3><%= projectName %></h3>
                            <a href="/<%= projectName %>" class="btn">localhost/<%= projectName %></a>
                        </div>
                        <% }); %>
                </div>
            </section>
        </div>
    </main>

    <footer>
        <p>By Alonso</p>
    </footer>
</body>

</html>